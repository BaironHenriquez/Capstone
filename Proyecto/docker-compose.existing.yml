version: "3.9"

services:
  # 🧩 Aplicación Laravel (PHP-FPM)
  app:
    build:
      context: .
      dockerfile: Dockerfile.existing
    image: laravel-app-existing
    container_name: laravel-app
    restart: unless-stopped
    working_dir: /var/www
    # ⚡ Usa :cached para mejorar la velocidad de acceso en Windows/macOS
    volumes:
      - ./:/var/www:cached
    networks:
      - laravel

  # 🌐 Servidor web Nginx
  webserver:
    image: nginx:alpine
    container_name: laravel-webserver
    restart: unless-stopped
    ports:
      - "8080:80"
    # ⚡ Usa :cached para reducir latencia de lectura de archivos
    volumes:
      - ./:/var/www:cached
      - ./docker-compose/nginx:/etc/nginx/conf.d
    networks:
      - laravel
    depends_on:
      - app

  # 🗄️ Base de datos MySQL
  db:
    image: mysql:8.0
    container_name: laravel-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: capstone_laravel
      MYSQL_ROOT_PASSWORD: capstone_root_2025
      MYSQL_USER: capstone_user
      MYSQL_PASSWORD: capstone_password_2025
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    # ⚡ Volumen persistente de datos + config
    volumes:
      - dbdata:/var/lib/mysql
      - ./docker-compose/mysql/my.cnf:/etc/mysql/my.cnf
      - ./docker-compose/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - laravel
    ports:
      - "3307:3306"

  # ⚡ Redis (cache, sesiones)
  redis:
    image: redis:alpine
    container_name: laravel-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - laravel

  # 💻 Node.js (para Vite y frontend)
  node:
    image: node:18-alpine
    container_name: laravel-node
    restart: "no"
    working_dir: /var/www
    # ⚡ Usa :cached para lectura rápida desde Windows
    volumes:
      - ./:/var/www:cached
    command: tail -f /dev/null
    ports:
      - "5173:5173"
    networks:
      - laravel
    depends_on:
      - app

  # 🧠 phpMyAdmin (administrar MySQL)
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: laravel-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_USER: capstone_user
      PMA_PASSWORD: capstone_password_2025
      MYSQL_ROOT_PASSWORD: capstone_root_2025
    ports:
      - "8081:80"
    networks:
      - laravel
    depends_on:
      - db

# 🌉 Red interna
networks:
  laravel:
    driver: bridge

# 💾 Volumen persistente de MySQL
volumes:
  dbdata:
    driver: local
